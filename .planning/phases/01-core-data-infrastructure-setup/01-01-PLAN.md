---
phase: 01-core-data-infrastructure-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data-sync/src/db/client.py
  - data-sync/src/models/ais.py
autonomous: true
requirements:
  - REQ-DB-001
  - REQ-DB-002
user_setup:
  - service: MongoDB
    why: "Persistent storage for ship data"
    env_vars:
      - name: MONGO_URI
        source: "Your MongoDB instance connection string (e.g., MongoDB Atlas, local Docker)"
    dashboard_config: []

must_haves:
  truths:
    - "A connection to MongoDB can be established from the `data-sync` service."
    - "AIS message data can be represented by a defined schema in Python (Pydantic models)."
    - "Basic CRUD operations (e.g., insert) on the defined schema can be performed successfully without errors."
  artifacts:
    - path: "data-sync/src/db/client.py"
      provides: "MongoDB connection utility"
      contains: "AsyncIOMotorClient"
    - path: "data-sync/src/models/ais.py"
      provides: "Pydantic models for AIS messages"
      contains: "BaseModel"
  key_links:
    - from: "data-sync/src/db/client.py"
      to: "MongoDB instance"
      via: "motor.motor_asyncio.AsyncIOMotorClient"
      pattern: "AsyncIOMotorClient"
---

<objective>
Establish the foundational connection to MongoDB and define the initial data models for AIS messages within the `data-sync` service.

Purpose: This plan lays the groundwork for persistent data storage, enabling subsequent plans to ingest and process real-time AIS data effectively.
Output: A functional MongoDB client and a robust Pydantic-based schema for AIS messages.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md

# AIS Message Structure Reference (simplified for planning - full details from `aisstream.io` docs)
# (Actual implementation will require detailed AIS message specifications from aisstream.io)
```json
{
  "MESSAGE_TYPE": {
    "Message3": {
      "MMSI": 123456789,
      "NAV_STATUS": 0,
      "SOG": 10.5,
      "POS_ACCURACY": 1,
      "LONGITUDE": -70.1234,
      "LATITUDE": 40.5678,
      "COG": 120.0,
      "TRUE_HEADING": 115,
      "TIMESTAMP": 1678886400,
      "SPARE": 0,
      "MANEUVER_INDICATOR": 0,
      "RAIM_FLAG": 0,
      "COMM_STATE": { ... }
    },
    "Message5": {
      "MMSI": 123456789,
      "AIS_VERSION": 2,
      "IMO": 9876543,
      "CALL_SIGN": "ABC1234",
      "VESSEL_NAME": "EXAMPLE SHIP",
      "VESSEL_TYPE": 70,
      "DIM_BOW": 10,
      "DIM_STERN": 100,
      "DIM_PORT": 5,
      "DIM_STARBOARD": 15,
      "FIX_TYPE": 1,
      "ETA_MONTH": 3,
      "ETA_DAY": 15,
      "ETA_HOUR": 10,
      "ETA_MINUTE": 30,
      "DRAUGHT": 12.5,
      "DESTINATION": "SOME PORT",
      "DTE": 0
    }
  },
  "META_DATA": {
    "time_utc": "2023-03-15T12:00:00Z",
    "mmsi": 123456789,
    "type": "AISMessage",
    "lon": -70.1234,
    "lat": 40.5678
  }
}
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Setup MongoDB connection in `data-sync`</name>
  <files>data-sync/src/db/client.py</files>
  <behavior>
    - Test 1: `connect_to_mongo` successfully establishes a connection.
    - Test 2: `close_mongo_connection` successfully closes the connection.
    - Test 3: Connection fails if `MONGO_URI` is invalid, raising a `ConnectionError`.
  </behavior>
  <action>
    Create `data-sync/src/db/client.py`. Implement an asynchronous function `connect_to_mongo` that takes a MongoDB URI (e.g., from an environment variable) and returns an `AsyncIOMotorClient` instance. Also, implement `close_mongo_connection` to gracefully close the client. Use `python-dotenv` to load environment variables from `.env`. Ensure proper error handling for connection failures.
    The MongoDB URI should be sourced from `os.getenv("MONGO_URI")`.
  </action>
  <verify>
    <automated>pytest data-sync/tests/db/test_client.py</automated>
  </verify>
  <done>
    `data-sync/src/db/client.py` contains functions to connect and disconnect from MongoDB.
    `MONGO_URI` environment variable is loaded and used for connection.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define Pydantic models for core AIS messages</name>
  <files>data-sync/src/models/ais.py</files>
  <behavior>
    - Test 1: `AISMessage3` model correctly validates and parses sample Message 3 JSON.
    - Test 2: `AISMessage5` model correctly validates and parses sample Message 5 JSON.
    - Test 3: An `AISBaseMessage` (or similar base class) correctly extracts common fields like MMSI, longitude, latitude, and timestamp.
    - Test 4: Invalid input JSON for any model raises a `ValidationError`.
  </behavior>
  <action>
    Create `data-sync/src/models/ais.py`. Define Pydantic `BaseModel` classes for common AIS message types, such as `AISMessage3` (position report) and `AISMessage5` (static and voyage data). Include fields like MMSI, latitude, longitude, speed, course, vessel name, IMO number, etc., based on typical AIS data. Create a base model `AISBaseMessage` to capture common fields across all AIS messages.
  </action>
  <verify>
    <automated>pytest data-sync/tests/models/test_ais.py</automated>
  </verify>
  <done>
    `data-sync/src/models/ais.py` contains Pydantic models for `AISMessage3`, `AISMessage5`, and a base `AISBaseMessage`.
    Models correctly validate sample AIS JSON data.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data-sync service <-> MongoDB | Untrusted data potentially crosses here (connection string, data for insertion) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | data-sync/src/db/client.py | mitigate | Ensure MongoDB connection string uses authentication (username/password) and is stored securely (environment variables). |
| T-01-01-02 | Information Disclosure | data-sync/src/db/client.py | mitigate | Restrict network access to MongoDB to only the `data-sync` service. |
</threat_model>

<verification>
Verify that the `data-sync` service can establish a connection to a MongoDB instance.
Verify that the defined Pydantic models accurately represent AIS message structures and can validate incoming data.
</verification>

<success_criteria>
- MongoDB connection utility is functional and resilient to basic connection issues.
- Pydantic models for at least `AISMessage3` and `AISMessage5` are defined and capable of validating representative AIS JSON payloads.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-data-infrastructure-setup/01-01-PLAN-SUMMARY.md`
</output>
