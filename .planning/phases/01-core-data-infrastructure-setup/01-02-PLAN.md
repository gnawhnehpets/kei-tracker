---
phase: 01-core-data-infrastructure-setup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - data-sync/src/ais/client.py
  - data-sync/src/ais/parser.py
autonomous: true
requirements:
  - REQ-RT-004
  - REQ-RT-005
user_setup:
  - service: aisstream.io
    why: "Real-time AIS data feed"
    env_vars:
      - name: AISSTREAM_API_KEY
        source: "Your API key from aisstream.io (if required for WebSocket connection)"
    dashboard_config: []

must_haves:
  truths:
    - "The `data-sync` service can connect to `aisstream.io` via WebSocket and receive raw AIS messages."
    - "Raw AIS JSON messages are correctly parsed into structured Python objects (Pydantic models)."
    - "The parsing logic handles various AIS message types defined in the schema."
  artifacts:
    - path: "data-sync/src/ais/client.py"
      provides: "AISStream.io WebSocket client"
      contains: "websockets.connect"
    - path: "data-sync/src/ais/parser.py"
      provides: "AIS message parsing functions"
      contains: "parse_ais_message"
  key_links:
    - from: "data-sync/src/ais/client.py"
      to: "aisstream.io"
      via: "websockets"
      pattern: "websockets\\.connect"
    - from: "data-sync/src/ais/parser.py"
      to: "data-sync/src/models/ais.py"
      via: "Pydantic model instantiation"
      pattern: "ais\\.AISMessage"
---

<objective>
Implement the WebSocket client for `aisstream.io` to ingest raw AIS messages and develop the core parsing logic to transform these raw messages into structured data.

Purpose: This plan establishes the primary data ingestion pipeline and the initial transformation step, crucial for turning raw external data into a usable format.
Output: A functional WebSocket client for `aisstream.io` and a robust AIS message parsing utility.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@.planning/phases/01-core-data-infrastructure-setup/01-01-PLAN-SUMMARY.md

# AISStream.io WebSocket Endpoint:
wss://stream.aisstream.io/v0/stream

# Sample Raw AIS JSON (from aisstream.io documentation):
```json
{
  "Message": {
    "Message3": {
      "MMSI": 123456789,
      "NAV_STATUS": 0,
      "SOG": 10.5,
      "POS_ACCURACY": 1,
      "LONGITUDE": -70.1234,
      "LATITUDE": 40.5678,
      "COG": 120.0,
      "TRUE_HEADING": 115,
      "TIMESTAMP": 1678886400,
      "SPARE": 0,
      "MANEUVER_INDICATOR": 0,
      "RAIM_FLAG": 0,
      "COMM_STATE": {
        "SYNC_STATE": 0,
        "SLOT_TIMEOUT": 0,
        "RECEIVE_SLOTS": 0
      }
    }
  },
  "MetaData": {
    "time_utc": "2023-03-15T12:00:00Z",
    "mmsi": 123456789,
    "type": "AISMessage",
    "lon": -70.1234,
    "lat": 40.5678
  }
}
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement WebSocket client for aisstream.io</name>
  <files>data-sync/src/ais/client.py</files>
  <behavior>
    - Test 1: `connect_to_aisstream` successfully establishes a WebSocket connection.
    - Test 2: `receive_ais_message` receives a valid JSON message from the mock WebSocket server.
    - Test 3: Connection fails if the WebSocket URL is invalid or unavailable, raising a `ConnectionError`.
  </behavior>
  <action>
    Create `data-sync/src/ais/client.py`. Implement an asynchronous function `connect_to_aisstream` that establishes a WebSocket connection to `wss://stream.aisstream.io/v0/stream`. This function should return a WebSocket client object. Also, implement `receive_ais_message` to asynchronously receive and return raw JSON messages from the WebSocket. Use the `websockets` library. Include basic error handling for connection issues.
    The `aisstream.io` API key should be sourced from `os.getenv("AISSTREAM_API_KEY")` and included in the connection (check `aisstream.io` docs for exact header/query parameter).
  </action>
  <verify>
    <automated>pytest data-sync/tests/ais/test_client.py</automated>
  </verify>
  <done>
    `data-sync/src/ais/client.py` contains functions for connecting to `aisstream.io` and receiving messages.
    The WebSocket connection is established, and raw messages can be received.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Develop a parser for raw AIS messages</name>
  <files>data-sync/src/ais/parser.py</files>
  <behavior>
    - Test 1: `parse_ais_message` successfully parses a sample `Message3` JSON into an `AISMessage3` Pydantic model.
    - Test 2: `parse_ais_message` successfully parses a sample `Message5` JSON into an `AISMessage5` Pydantic model.
    - Test 3: `parse_ais_message` handles unknown message types gracefully (e.g., returns `None` or raises a specific error).
    - Test 4: Invalid JSON input raises an appropriate error (`json.JSONDecodeError` or `ValueError`).
  </behavior>
  <action>
    Create `data-sync/src/ais/parser.py`. Implement a function `parse_ais_message` that takes a raw AIS JSON string (received from the WebSocket client) and attempts to parse it into the appropriate Pydantic model defined in `data-sync/src/models/ais.py`. The parser should identify the message type (e.g., "Message3", "Message5") from the JSON structure and instantiate the corresponding Pydantic model. Include error handling for malformed JSON or unknown message types.
  </action>
  <verify>
    <automated>pytest data-sync/tests/ais/test_parser.py</automated>
  </verify>
  <done>
    `data-sync/src/ais/parser.py` contains a function `parse_ais_message` that can parse raw AIS JSON into Pydantic models.
    The parser can correctly identify and parse `Message3` and `Message5` types.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data-sync service <-> aisstream.io | External data stream and potential for malicious/malformed input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Spoofing | data-sync/src/ais/client.py | mitigate | Verify the source of WebSocket messages if possible (though `aisstream.io` is a trusted provider, general practice). |
| T-01-02-02 | Tampering | data-sync/src/ais/parser.py | mitigate | Implement robust input validation during parsing to detect and reject malformed or unexpected AIS message data. |
| T-01-02-03 | Denial of Service | data-sync/src/ais/client.py | mitigate | Implement rate limiting and connection management to prevent resource exhaustion from too many or malformed incoming messages. |
</threat_model>

<verification>
Verify that the WebSocket client successfully connects to `aisstream.io` and receives continuous raw AIS messages.
Verify that the parser can take these raw messages and consistently transform them into structured Pydantic objects without errors.
</verification>

<success_criteria>
- WebSocket client successfully connects to `aisstream.io` and maintains a stable connection, receiving messages.
- AIS message parsing logic correctly identifies and parses common AIS message types into defined Pydantic models.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-data-infrastructure-setup/01-02-PLAN-SUMMARY.md`
</output>
