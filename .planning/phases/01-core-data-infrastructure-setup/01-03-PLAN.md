---
phase: 01-core-data-infrastructure-setup
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - data-sync/main.py
  - .env
autonomous: true
requirements:
  - REQ-DB-001
  - REQ-NF-009
user_setup: []

must_haves:
  truths:
    - "The `data-sync` service continuously ingests, parses, and stores real-time AIS messages into MongoDB."
    - "Errors during WebSocket connection or MongoDB operations are caught and handled gracefully (e.g., retries, logging)."
    - "MongoDB connection uses environment variables for credentials and is not publicly exposed."
  artifacts:
    - path: "data-sync/main.py"
      provides: "Main data synchronization loop"
      contains: "asyncio.run"
    - path: ".env"
      provides: "Environment variables for MongoDB and AISStream credentials"
      contains: "MONGO_URI, AISSTREAM_API_KEY"
  key_links:
    - from: "data-sync/main.py"
      to: "data-sync/src/ais/client.py"
      via: "call to start WebSocket client"
      pattern: "ais_client\\.connect"
    - from: "data-sync/main.py"
      to: "data-sync/src/ais/parser.py"
      via: "call to parse raw messages"
      pattern: "ais_parser\\.parse_ais_message"
    - from: "data-sync/main.py"
      to: "data-sync/src/db/client.py"
      via: "call to insert data into MongoDB"
      pattern: "db_client\\.insert_message"
    - from: "data-sync/main.py"
      to: ".env"
      via: "os.getenv"
      pattern: "os\\.getenv"
---

<objective>
Integrate all components into the `data-sync` service's main loop to continuously ingest, parse, and store AIS messages in MongoDB. This plan also includes implementing robust error handling and basic database security measures.

Purpose: This plan brings together all the foundational pieces, creating a complete and resilient data ingestion and storage pipeline, while also addressing critical security concerns for the database.
Output: A fully operational `data-sync` service that continuously populates MongoDB with processed AIS data, with built-in resilience and basic security.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@.planning/phases/01-core-data-infrastructure-setup/01-01-PLAN-SUMMARY.md
@.planning/phases/01-core-data-infrastructure-setup/01-02-PLAN-SUMMARY.md

# Relevant files from previous plans:
- `data-sync/src/db/client.py` (MongoDB connection)
- `data-sync/src/models/ais.py` (Pydantic AIS models)
- `data-sync/src/ais/client.py` (AISStream WebSocket client)
- `data-sync/src/ais/parser.py` (AIS message parser)

# Environment variables to be configured in .env:
```
MONGO_URI="mongodb://user:password@host:port/database"
AISSTREAM_API_KEY="your_aisstream_api_key"
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement main data synchronization loop</name>
  <files>data-sync/main.py</files>
  <action>
    Modify `data-sync/main.py`. Implement an asynchronous `main` function that orchestrates the data synchronization process. This function should:
    1. Connect to MongoDB using `data-sync/src/db/client.py`.
    2. Connect to `aisstream.io` via WebSocket using `data-sync/src/ais/client.py`.
    3. Continuously receive raw AIS messages using `data-sync/src/ais/client.py`.
    4. Parse each raw message using `data-sync/src/ais/parser.py`.
    5. Store the parsed (Pydantic) AIS messages into MongoDB.
    6. Ensure the `main` function is the entry point for the `data-sync` service using `asyncio.run()`.
  </action>
  <verify>
    <automated>python data-sync/main.py --test-run --duration 10</automated>
  </verify>
  <done>
    `data-sync/main.py` executes, connects to both services, and continuously ingests and stores AIS messages in MongoDB for at least 10 seconds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate error handling, logging, and retry mechanisms</name>
  <files>data-sync/main.py</files>
  <action>
    Enhance `data-sync/main.py` by integrating robust error handling, logging, and retry mechanisms.
    Specifically:
    - Implement `try-except` blocks around WebSocket connection and message reception to handle network errors and unexpected disconnections with exponential backoff retries.
    - Implement `try-except` blocks around MongoDB operations to handle database connection issues and write errors.
    - Use Python's `logging` module to log significant events (connection status, errors, successful data persistence) with timestamps.
    - Ensure `python-dotenv` is properly configured to load environment variables for credentials (MONGO_URI, AISSTREAM_API_KEY).
  </action>
  <verify>
    <automated>python data-sync/main.py --simulate-errors --duration 30</automated>
  </verify>
  <done>
    The `data-sync` service logs connection attempts, retries, and data persistence events.
    The service demonstrates resilience by recovering from simulated network/database errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement basic database security</name>
  <files>data-sync/main.py, .env</files>
  <action>
    Review and update the MongoDB connection string usage in `data-sync/main.py` (via `data-sync/src/db/client.py` if not already done) to explicitly use username/password authentication. Ensure these credentials are sourced from environment variables (e.g., `MONGO_USERNAME`, `MONGO_PASSWORD`) defined in the `.env` file. Add these new variables to the `.env` file example.

    If not already configured, ensure the `data-sync` Dockerfile explicitly exposes only necessary ports and the MongoDB instance is configured with appropriate network access controls (e.g., firewall rules, VPC peering) to restrict access to only the `data-sync` service.
  </action>
  <verify>
    <automated>Manual verification: Inspect MongoDB connection string in logs (obscured) and confirm network rules for MongoDB instance. (automated tests for connection string, not network rules)</automated>
    <automated>pytest data-sync/tests/db/test_security.py</automated>
  </verify>
  <done>
    MongoDB connection uses environment variables for username/password.
    The `.env` file template includes `MONGO_USERNAME` and `MONGO_PASSWORD`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data-sync service <-> MongoDB | Continuous data flow, critical credentials involved |
| data-sync service <-> .env | Sensitive configuration storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Information Disclosure | .env | mitigate | Ensure `.env` is properly excluded from version control and deployments (e.g., via `.gitignore`). |
| T-01-03-02 | Tampering | data-sync/main.py | mitigate | Ensure all data written to MongoDB undergoes final validation before persistence. |
| T-01-03-03 | Elevation of Privilege | MongoDB connection | mitigate | Use principle of least privilege for MongoDB user accounts, granting only necessary read/write permissions. |
</threat_model>

<verification>
Verify that the `data-sync` service runs continuously without crashing, successfully processing and storing AIS messages in MongoDB.
Verify that error conditions (e.g., network outage, database unavailability) are handled gracefully with retries and informative logging.
Verify that MongoDB connection credentials are securely loaded from environment variables.
</verification>

<success_criteria>
- The `data-sync` service successfully ingests, parses, and persists real-time AIS messages into MongoDB, demonstrating resilience to transient errors.
- Comprehensive logging provides insight into the service's operational status and error events.
- MongoDB connection utilizes secure authentication via environment variables.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-data-infrastructure-setup/01-03-PLAN-SUMMARY.md`
</output>
